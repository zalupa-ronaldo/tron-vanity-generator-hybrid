#version 450

layout(local_size_x = 64) in;
layout(std430, set = 0, binding = 0) buffer ProbeData {
    uint values[];
} data;

void main() {
    uint i = gl_GlobalInvocationID.x;
    if (i < 256u)
        data.values[i] = (data.values[i] ^ 0x9e3779b9u) * 1664525u + 1013904223u;
}
